const { processing } = require('./UserSql')
function exchange(params) {
    return new Promise((reslove, reject) => {
        let sql = ``
        processing([], sql, (data) => {

        })
    })
}
module.exports = {
    exchange

}